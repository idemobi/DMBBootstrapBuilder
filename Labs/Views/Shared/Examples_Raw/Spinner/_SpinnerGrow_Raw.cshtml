<div class="dmb-demo-code border rounded-3 overflow-hidden bg-body"><div class="dmb-demo-code-header px-3 py-2 border-bottom bg-body-tertiary d-flex align-items-center"><span class="badge text-bg-secondary font-monospace">C#</span></div><pre class="language-csharp mb-0 border-0 rounded-0"><code class="language-csharp">@@using DMBBootstrapBuilder
@@using DMBPageBuilder
@@using (Html.FlexBlockBuilder().SetAlignItemsCenter().WithGap(Old_Gap.Gap3).Begin())
{
    @@using (Html.BlockBuilder().AsTextCenter().Begin())
    {
        @@Html.Spinner().SetSpinnerType(SpinnerType.Grow).SetVariant(VariantStyle.Primary)
        @@using (Html.PB_P().AsMuted().Begin())
        {
            @@:Default
        }
    }
    @@using (Html.BlockBuilder().AsTextCenter().Begin())
    {
        @@Html.Spinner().SetSpinnerType(SpinnerType.Grow).SetVariant(VariantStyle.Info).SetSize(SpinnerSize.Small)
        @@using (Html.PB_P().AsMuted().Begin())
        {
            @@:Small
        }
    }
    @@using (Html.BlockBuilder().AsTextCenter().Begin())
    {
        @@Html.Spinner().SetSpinnerType(SpinnerType.Grow).SetVariant(VariantStyle.Success).SetSize(40, UnitSize.px)
        @@using (Html.PB_P().AsMuted().Begin())
        {
            @@:40 px
        }
    }
    @@using (Html.BlockBuilder().AsTextCenter().Begin())
    {
        @@Html.Spinner().SetSpinnerType(SpinnerType.Grow).SetVariant(VariantStyle.Warning).SetLabel("Loading data")
        @@using (Html.PB_P().AsMuted().Begin())
        {
            @@:Custom label
        }
    }
}</code></pre></div>
@(Html.AlertBuilder()
    .SetTitle("Deployment complete")
    .SetMessage("All 3 services are running on production.")
    .SetVariant(VariantStyle.Success)
    .Render())
@(Html.AlertBuilder()
    .SetTitle("Billing issue")
    .SetMessage("Your payment method was declined. Update it before the next cycle.")
    .SetVariant(VariantStyle.Danger)
    .SetIcon(IconStruct.Bootstrap("bi-exclamation-octagon"))
    .Render())

@using (Html.AccordionAreaBuilder().Id("acc-flush").Flush().Begin())
{
    @using (Html.AccordionBlockBuilder().WithTitle("Overview").WithIcon(IconStruct.Bootstrap("bi-bar-chart")).Open().Begin())
    {
        @using (Html.PB_P().AsMuted().Begin())
        {
            @:High-level summary of the project status and key metrics.
        }
    }
    @using (Html.AccordionBlockBuilder().WithTitle("Team").WithIcon(IconStruct.Bootstrap("bi-people")).Begin())
    {
        @using (Html.PB_P().AsMuted().Begin())
        {
            @:Jean-François · Quentin · Lila · Gégé
        }
    }
    @using (Html.AccordionBlockBuilder().WithTitle("Billing").WithIcon(IconStruct.Bootstrap("bi-credit-card")).Begin())
    {
        @using (Html.PB_P().AsMuted().Begin())
        {
            @:Billing information and current subscription status.
        }
    }
}

@{
    Layout = "_Layout";
    string DocUrl(string c) => $"/Documentation/Show?packageId=DMBBootstrapBuilder&groupName=NuGet&namespaceName=DMBBootstrapBuilder&objectName={c}";
}

@using (Html.SectionBuilder().SetHeight(125, UnitSize.px).BootstrapBackgroundEffect(VariantStyle.Normal).Begin())
{
    @using (Html.FlexBlockBuilder().SetAlignItemsCenter().SetJustifyContentCenter().Begin())
    {
        @using (Html.BlockBuilder().AsTextCenter().Begin())
        {
            @Html.TitleBuilder().SetTitle("Badge", TitleLevel.One).Render()
            @using (Html.PB_P().AsMuted().Begin())
            {
                @:Small count or label chips — variants, pill shape, positioned on buttons and headings.
            }
        }
    }
}

@* ── Basic ── *@
@using (Html.BlockBuilder().AsSectionHeader().Begin())
{
    @using (Html.BlockBuilder().Begin())
    {
        @Html.TitleBuilder().SetTitle("Basic", TitleLevel.Three).Render()
        @using (Html.PB_P().AsMuted().Begin())
        {
            @:Html.BadgeBuilder(text) renders a span badge. SetBadgeVariant(VariantStyle) applies a Bootstrap colour.
        }
    }
    @Html.Button(ActionItemFactory.Url(title: "", url: DocUrl(nameof(BadgeBuilder)), icon: IconStruct.Bootstrap("bi-book-half")).SetVariant(VariantStyle.Secondary).SetOutlined()).Render()
}

@using (Html.BlockBuilder().AsSectionContent().Begin())
{
    @using (Html.BlockBuilder().AsExamplePreview().Begin())
    {
        @await Html.PartialAsync("~/Views/Shared/Examples/Badge/_BadgeVariantsGallery.cshtml")
    }
    @await Html.RenderExamplePartialAsync("Examples/Badge/_BasicBadge", "Basic badge", IconStruct.Bootstrap("bi-tag"))
}

@* ── Pill ── *@
@using (Html.BlockBuilder().AsSectionHeader().Begin())
{
    @using (Html.BlockBuilder().Begin())
    {
        @Html.TitleBuilder().SetTitle("Pill", TitleLevel.Three).Render()
        @using (Html.PB_P().AsMuted().Begin())
        {
            @:SetBadgePill() applies rounded corners — useful for counts and tag-like labels.
        }
    }
    @Html.Button(ActionItemFactory.Url(title: "", url: DocUrl(nameof(BadgeBuilder)), icon: IconStruct.Bootstrap("bi-book-half")).SetVariant(VariantStyle.Secondary).SetOutlined()).Render()
}

@using (Html.BlockBuilder().AsSectionContent().Begin())
{
    @using (Html.BlockBuilder().AsExamplePreview().Begin())
    {
        @await Html.PartialAsync("~/Views/Shared/Examples/Badge/_BadgePillVariantsGallery.cshtml")
    }
    @await Html.RenderExamplePartialAsync("Examples/Badge/_PillBadge", "Pill badge", IconStruct.Bootstrap("bi-tag-fill"))
}

@* ── In headings ── *@
@using (Html.BlockBuilder().AsSectionHeader().Begin())
{
    @using (Html.BlockBuilder().Begin())
    {
        @Html.TitleBuilder().SetTitle("In headings", TitleLevel.Three).Render()
        @using (Html.PB_P().AsMuted().Begin())
        {
            @:Badges placed inline inside a heading inherit the surrounding font size automatically.
        }
    }
    @Html.Button(ActionItemFactory.Url(title: "", url: DocUrl(nameof(BadgeBuilder)), icon: IconStruct.Bootstrap("bi-book-half")).SetVariant(VariantStyle.Secondary).SetOutlined()).Render()
}

@using (Html.BlockBuilder().AsSectionContent().Begin())
{
    @await Html.RenderExamplePartialAsync("Examples/Badge/_BadgeInHeadings", "In headings", IconStruct.Bootstrap("bi-type-h1"))
}

@* ── Button notification ── *@
@using (Html.BlockBuilder().AsSectionHeader().Begin())
{
    @using (Html.BlockBuilder().Begin())
    {
        @Html.TitleBuilder().SetTitle("Button notification", TitleLevel.Three).Render()
        @using (Html.PB_P().AsMuted().Begin())
        {
            @:SetHiddenText(string) adds a visually-hidden accessibility label. The badge sits naturally after the button text.
        }
    }
    @Html.Button(ActionItemFactory.Url(title: "", url: DocUrl(nameof(BadgeBuilder)), icon: IconStruct.Bootstrap("bi-book-half")).SetVariant(VariantStyle.Secondary).SetOutlined()).Render()
}

@using (Html.BlockBuilder().AsSectionContent().Begin())
{
    @await Html.RenderExamplePartialAsync("Examples/Badge/_ButtonBadge", "Button badge", IconStruct.Bootstrap("bi-bell"))
}

@* ── Positioned ── *@
@using (Html.BlockBuilder().AsSectionHeader().Begin())
{
    @using (Html.BlockBuilder().Begin())
    {
        @Html.TitleBuilder().SetTitle("Positioned", TitleLevel.Three).Render()
        @using (Html.PB_P().AsMuted().Begin())
        {
            @:SetPosition(Position.Absolute) with SetTop, SetStart and SetTranslate places the badge on the button corner.
        }
    }
    @Html.Button(ActionItemFactory.Url(title: "", url: DocUrl(nameof(BadgeBuilder)), icon: IconStruct.Bootstrap("bi-book-half")).SetVariant(VariantStyle.Secondary).SetOutlined()).Render()
}

@using (Html.BlockBuilder().AsSectionContent().Begin())
{
    @await Html.RenderExamplePartialAsync("Examples/Badge/_BadgePositioned", "Positioned badge", IconStruct.Bootstrap("bi-pin-angle"))
}

@* ── Indicator dot ── *@
@using (Html.BlockBuilder().AsSectionHeader().Begin())
{
    @using (Html.BlockBuilder().Begin())
    {
        @Html.TitleBuilder().SetTitle("Indicator dot", TitleLevel.Three).Render()
        @using (Html.PB_P().AsMuted().Begin())
        {
            @:SetAsNotification() renders a small dot with no visible text — SetHiddenText provides an accessible label.
        }
    }
    @Html.Button(ActionItemFactory.Url(title: "", url: DocUrl(nameof(BadgeBuilder)), icon: IconStruct.Bootstrap("bi-book-half")).SetVariant(VariantStyle.Secondary).SetOutlined()).Render()
}

@using (Html.BlockBuilder().AsSectionContent().Begin())
{
    @await Html.RenderExamplePartialAsync("Examples/Badge/_BadgeIndicator", "Indicator dot", IconStruct.Bootstrap("bi-record-circle"))
}
